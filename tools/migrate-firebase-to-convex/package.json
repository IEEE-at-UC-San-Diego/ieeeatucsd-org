{
  "name": "@ieeeatucsd/migrate-firebase-to-convex",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "bun run src/index.ts",
    "migrate:dry-run": "bun run src/index.ts --dry-run",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit",
    "test": "tsc --noEmit"
  },
  "dependencies": {
    "convex": "catalog:",
    "dotenv": "^16.4.7",
    "firebase-admin": "^13.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
